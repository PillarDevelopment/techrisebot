# 🚀 Развертывание на удаленном сервере

Подробная инструкция по запуску Telegram бота на удаленном сервере.

## 📋 Требования

- Сервер с Linux (Ubuntu/Debian/CentOS)
- Python 3.9 или выше
- Доступ по SSH
- Токен Telegram бота
- Ваш Telegram User ID

## 🔧 Вариант 1: Использование systemd (Рекомендуется)

Этот вариант обеспечивает автоматический запуск бота при перезагрузке сервера и автоматический перезапуск при сбоях.

### Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу по SSH
ssh user@your-server.com

# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Python и pip (если еще не установлены)
sudo apt install python3 python3-pip python3-venv -y
```

### Шаг 2: Загрузка проекта на сервер

```bash
# Создайте директорию для проекта
mkdir -p ~/techrisebot
cd ~/techrisebot

# Загрузите файлы проекта (используйте scp, git или другой метод)
# Например, через scp с локального компьютера:
# scp -r /Users/ivanborisov/Desktop/techrisebot/* user@your-server.com:~/techrisebot/

# Или клонируйте из git репозитория (если есть)
# git clone https://github.com/yourusername/techrisebot.git
# cd techrisebot
```

### Шаг 3: Установка зависимостей

```bash
# Создайте виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

```bash
# Создайте файл .env
nano .env
```

Добавьте в файл:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_USER_ID=ваш_telegram_id
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

### Шаг 5: Создание systemd сервиса

```bash
# Создайте файл сервиса
sudo nano /etc/systemd/system/techrisebot.service
```

Добавьте следующее содержимое (замените `your_username` на ваше имя пользователя):

```ini
[Unit]
Description=TechRise Goals Tracker Telegram Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/home/your_username/techrisebot
Environment="PATH=/home/your_username/techrisebot/venv/bin"
ExecStart=/home/your_username/techrisebot/venv/bin/python3 /home/your_username/techrisebot/bot.py
Restart=always
RestartSec=10
StandardOutput=append:/home/your_username/techrisebot/bot.log
StandardError=append:/home/your_username/techrisebot/bot.log

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `your_username` на ваше реальное имя пользователя на сервере!

### Шаг 6: Запуск сервиса

```bash
# Перезагрузите systemd для чтения нового сервиса
sudo systemctl daemon-reload

# Включите автозапуск при загрузке системы
sudo systemctl enable techrisebot

# Запустите сервис
sudo systemctl start techrisebot

# Проверьте статус
sudo systemctl status techrisebot
```

### Управление сервисом

```bash
# Остановить бота
sudo systemctl stop techrisebot

# Запустить бота
sudo systemctl start techrisebot

# Перезапустить бота
sudo systemctl restart techrisebot

# Посмотреть логи
sudo journalctl -u techrisebot -f
# Или логи из файла
tail -f ~/techrisebot/bot.log
```

## 🔧 Вариант 2: Использование screen/tmux (Простой вариант)

Если у вас нет прав sudo или вы хотите более простой вариант.

### Шаг 1-4: Выполните те же шаги подготовки, что и в варианте 1

### Шаг 5: Запуск в screen

```bash
# Установите screen (если еще не установлен)
sudo apt install screen -y

# Создайте новую сессию screen
screen -S techrisebot

# Активируйте виртуальное окружение (если используете)
source venv/bin/activate

# Запустите бота
python3 bot.py

# Отключитесь от screen: нажмите Ctrl+A, затем D
```

### Подключение к сессии

```bash
# Посмотреть список сессий
screen -ls

# Подключиться к сессии
screen -r techrisebot

# Выйти из screen (остановив бота): нажмите Ctrl+C, затем введите exit
```

### Альтернатива: tmux

```bash
# Установите tmux
sudo apt install tmux -y

# Создайте новую сессию
tmux new -s techrisebot

# Запустите бота
source venv/bin/activate
python3 bot.py

# Отключитесь: нажмите Ctrl+B, затем D

# Подключиться обратно
tmux attach -t techrisebot
```

## 🔧 Вариант 3: Использование nohup (Минимальный вариант)

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота в фоне
nohup python3 bot.py > bot.log 2>&1 &

# Проверьте, что бот запущен
ps aux | grep bot.py

# Посмотрите логи
tail -f bot.log

# Остановить бота (найдите PID и убейте процесс)
ps aux | grep bot.py
kill <PID>
```

## 🔍 Проверка работы

1. **Проверьте логи:**
   ```bash
   # Для systemd
   sudo journalctl -u techrisebot -n 50
   
   # Для screen/tmux/nohup
   tail -f bot.log
   ```

2. **Проверьте в Telegram:**
   - Напишите боту команду `/start`
   - Бот должен ответить

3. **Проверьте базу данных:**
   ```bash
   ls -lh goals.db
   ```

## 🛠️ Решение проблем

### Бот не запускается

```bash
# Проверьте логи ошибок
sudo journalctl -u techrisebot -n 100

# Проверьте, что файл .env существует и заполнен
cat .env

# Проверьте права доступа
chmod 600 .env
```

### Бот запускается, но не отвечает

1. Проверьте токен в `.env`
2. Проверьте, что бот не заблокирован в Telegram
3. Проверьте интернет-соединение на сервере

### Уведомления не приходят

1. Проверьте `TELEGRAM_USER_ID` в `.env`
2. Убедитесь, что уведомления включены: `/remind on`
3. Проверьте логи планировщика

### Ошибка "Permission denied"

```bash
# Дайте права на выполнение
chmod +x bot.py

# Проверьте права на директорию
ls -la
```

## 📝 Обновление бота

```bash
# Остановите бота
sudo systemctl stop techrisebot  # для systemd
# или найдите процесс и убейте его для других вариантов

# Загрузите новые файлы
# (через git pull, scp и т.д.)

# Обновите зависимости (если нужно)
source venv/bin/activate
pip install -r requirements.txt

# Запустите бота снова
sudo systemctl start techrisebot
```

## 🔒 Безопасность

1. **Не коммитьте `.env` в git:**
   ```bash
   # Добавьте в .gitignore
   echo ".env" >> .gitignore
   echo "*.db" >> .gitignore
   echo "venv/" >> .gitignore
   ```

2. **Ограничьте права доступа к `.env`:**
   ```bash
   chmod 600 .env
   ```

3. **Используйте виртуальное окружение** для изоляции зависимостей

4. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# Использование памяти и CPU
top -p $(pgrep -f bot.py)

# Или с htop (если установлен)
htop
```

### Автоматический мониторинг (опционально)

Можно настроить мониторинг через cron или другие инструменты для проверки, что бот работает.

## 🎯 Готово!

Теперь ваш бот должен работать на удаленном сервере 24/7. Проверьте его работу, написав `/start` в Telegram!

