# 🎯 Goals Tracker Bot 2026

Telegram бот для отслеживания прогресса по целям на 2026 год. Бот помогает отслеживать финансовые цели, спорт, покупки и путешествия, отправляя ежедневные напоминания и отчеты.

## 📋 Возможности

- ✅ Отслеживание прогресса по всем целям
- ✅ Ежедневные уведомления в 9:00 и 21:00 (МСК)
- ✅ Команды для обновления прогресса
- ✅ Недельные отчеты
- ✅ Хранение данных в SQLite базе данных

## 🚀 Быстрый старт

### Шаг 1: Создать бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например: "Goals Tracker 2026")
4. Придумайте username (например: `ivan_goals_2026_bot`)
5. Скопируйте токен (выглядит как `123456:ABC-DEF...`)

### Шаг 2: Узнать свой Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot) команду `/start`
2. Он покажет ваш Telegram ID (число вида `123456789`)

### Шаг 3: Установить зависимости

```bash
# Убедитесь, что у вас установлен Python 3.9 или выше
python3 --version

# Установите зависимости
# На macOS используйте pip3, на Linux может быть просто pip
pip3 install -r requirements.txt
```

**Примечание для macOS:** На macOS команда `python` обычно не доступна, используйте `python3` вместо `python`.

### Шаг 4: Настроить переменные окружения

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте файл `.env` и заполните его:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_USER_ID=ваш_telegram_id
```

### Шаг 5: Запустить бота

```bash
python3 bot.py
```

Вы должны увидеть сообщение:
```
Бот запущен и готов к работе!
```

Теперь напишите боту команду `/start` в Telegram!

## 📱 Команды бота

### `/start`
Приветствие и список доступных команд

### `/today`
Показывает сводку на сегодня:
- Текущий день года
- Ближайшие дедлайны
- Прогресс по категориям

### `/goals`
Показывает список всех целей с их статусом:
- Текущее значение / целевое значение
- Оставшиеся дни до дедлайна
- Процент выполнения

### `/update [категория] [значение]`
Обновляет прогресс по цели.

**Примеры:**
- `/update вес 85` - обновить текущий вес
- `/update доход 500000` - обновить доход за месяц
- `/update тренировки 3` - обновить количество тренировок за неделю
- `/update страны +1` - добавить одну посещенную страну

**Доступные категории:**
- `вес` - текущий вес
- `доход` - доход за месяц
- `тренировки` - количество тренировок в неделю
- `страны` - количество новых стран
- `марафоны` - количество пробежанных марафонов
- `машина` - покупка машины (1 = куплена)
- `квартира` - покупка квартиры (сумма в рублях)

### `/report`
Показывает недельный отчет:
- Статистика по тренировкам
- Доход за неделю
- Прогресс по целям

### `/log [текст]`
Добавляет запись в дневник прогресса.

**Пример:**
```
/log Сегодня отлично потренировался и заработал 100000 рублей!
```

### `/remind [on/off]`
Включает или выключает ежедневные уведомления.

**Примеры:**
- `/remind on` - включить уведомления
- `/remind off` - выключить уведомления

### `/skip`
Пропускает вечерний опрос (если не хотите отвечать на вопросы)

## ⏰ Уведомления

Бот автоматически отправляет уведомления:

- **9:00 МСК** - Утреннее напоминание с фокусом на день
- **21:00 МСК** - Вечерний опрос о прошедшем дне

## 🗄️ База данных

Бот использует SQLite базу данных (`goals.db`), которая создается автоматически при первом запуске.

**Таблицы:**
- `goals` - список целей
- `progress_log` - история изменений прогресса
- `daily_checkins` - ежедневные отметки
- `settings` - настройки бота

## 📊 Отслеживаемые цели

### Финансы 💰
- Доход 1М/мес к февралю 2026
- Доход 2М/мес к маю 2026
- Доход 5М/мес к ноябрю 2026
- 3-4 источника дохода к середине года

### Спорт 🏃
- Вес: с 87 до 80 кг к середине года
- 3-4 тренировки в неделю
- 2 марафона (май и сентябрь)

### Покупки 🛒
- Voyah Free (авто) к середине года
- Квартира в Москве (25 млн ₽) к середине года

### Путешествия ✈️
- 12 новых стран за год

## 🔧 Структура проекта

```
goals-tracker-bot/
├── bot.py              # Главный файл бота
├── database.py         # Работа с SQLite
├── goals.py            # Логика расчета прогресса
├── scheduler.py        # Планировщик уведомлений
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env                # Переменные окружения (создать самостоятельно)
├── .env.example        # Пример файла .env
├── README.md           # Документация
└── goals.db            # База данных (создается автоматически)
```

## 🐛 Решение проблем

### Бот не отвечает
1. Проверьте, что токен в `.env` правильный
2. Убедитесь, что бот запущен (`python bot.py`)
3. Проверьте логи на наличие ошибок

### Уведомления не приходят
1. Проверьте, что `TELEGRAM_USER_ID` правильный
2. Убедитесь, что уведомления включены (`/remind on`)
3. Проверьте, что планировщик запущен (должно быть в логах)

### Ошибка при установке зависимостей
```bash
# Попробуйте обновить pip
pip3 install --upgrade pip

# Затем установите зависимости снова
pip3 install -r requirements.txt
```

## 📝 Для постоянной работы на сервере

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/goals-tracker-bot.service`:

```ini
[Unit]
Description=Goals Tracker Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/goals-tracker-bot
ExecStart=/usr/bin/python3 /путь/к/goals-tracker-bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable goals-tracker-bot
sudo systemctl start goals-tracker-bot
```

### Использование nohup

```bash
nohup python3 bot.py > bot.log 2>&1 &
```

## 📄 Лицензия

Этот проект создан для личного использования.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Правильность заполнения `.env` файла
2. Логи бота на наличие ошибок
3. Версию Python (должна быть 3.11+)

---

**Удачи в достижении целей! 💪🎯**

