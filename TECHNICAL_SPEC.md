# 📋 Техническое задание: Миграция на Supabase и мультипользовательская система

## 🎯 Цели проекта

1. **Миграция базы данных**: Перенос с SQLite на Supabase (PostgreSQL)
2. **Мультипользовательская система**: Поддержка множественных пользователей с изоляцией данных
3. **Авторизация через Telegram**: Использование Telegram ID для аутентификации пользователей

---

## 📊 Текущее состояние системы

### Текущая архитектура:
- **База данных**: SQLite (локальный файл `goals.db`)
- **Аутентификация**: Один пользователь через `TELEGRAM_USER_ID` в `.env`
- **Хранение данных**: Локальные файлы на сервере
- **Масштабируемость**: Ограничена одним пользователем

### Ограничения текущей системы:
- ❌ Нет поддержки множественных пользователей
- ❌ Данные хранятся локально (риск потери при сбое сервера)
- ❌ Нет резервного копирования
- ❌ Нет возможности синхронизации между устройствами
- ❌ Сложность масштабирования

---

## 🏗️ Целевая архитектура

### Компоненты системы:

```
┌─────────────────┐
│  Telegram Bot   │
│   (Python)      │
└────────┬────────┘
         │
         │ HTTPS/API
         │
┌────────▼────────┐
│   Supabase      │
│   PostgreSQL    │
│   + Auth        │
└─────────────────┘
```

### Технологический стек:

- **Backend**: Python 3.9+ (текущий бот)
- **База данных**: Supabase PostgreSQL
- **Аутентификация**: Telegram User ID (через Supabase Auth или кастомная)
- **ORM/DB Client**: `supabase-py` или `psycopg2` + SQL
- **Миграции**: Supabase Migrations или Python скрипты

---

## 📐 Архитектура базы данных

### 1. Таблица `users`

Хранение информации о пользователях Telegram.

```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    telegram_user_id BIGINT UNIQUE NOT NULL,
    telegram_username VARCHAR(255),
    first_name VARCHAR(255),
    last_name VARCHAR(255),
    language_code VARCHAR(10) DEFAULT 'ru',
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    CONSTRAINT unique_telegram_user UNIQUE (telegram_user_id)
);

CREATE INDEX idx_users_telegram_id ON users(telegram_user_id);
```

**Поля:**
- `telegram_user_id` - уникальный ID пользователя из Telegram (используется для авторизации)
- `telegram_username` - @username пользователя (опционально)
- `first_name`, `last_name` - имя пользователя из Telegram
- `language_code` - язык интерфейса
- `is_active` - флаг активности (для блокировки пользователей)

### 2. Таблица `goals`

Цели пользователей (адаптирована под мультипользовательскую систему).

```sql
CREATE TABLE goals (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    category VARCHAR(50) NOT NULL,
    name VARCHAR(255) NOT NULL,
    target_value DECIMAL(15,2) NOT NULL,
    current_value DECIMAL(15,2) DEFAULT 0,
    initial_value DECIMAL(15,2),
    unit VARCHAR(50),
    deadline DATE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    CONSTRAINT unique_user_goal UNIQUE (user_id, category, name)
);

CREATE INDEX idx_goals_user_id ON goals(user_id);
CREATE INDEX idx_goals_category ON goals(user_id, category);
CREATE INDEX idx_goals_deadline ON goals(user_id, deadline);
```

**Изменения:**
- Добавлено поле `user_id` для связи с пользователем
- Уникальность по комбинации `(user_id, category, name)`
- Индексы для оптимизации запросов

### 3. Таблица `progress_log`

История изменений прогресса по целям.

```sql
CREATE TABLE progress_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    goal_id UUID NOT NULL REFERENCES goals(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    value DECIMAL(15,2) NOT NULL,
    note TEXT,
    logged_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_progress_log_goal_id ON progress_log(goal_id);
CREATE INDEX idx_progress_log_user_id ON progress_log(user_id);
CREATE INDEX idx_progress_log_date ON progress_log(logged_at DESC);
```

### 4. Таблица `daily_checkins`

Ежедневные отметки пользователей.

```sql
CREATE TABLE daily_checkins (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    date DATE NOT NULL,
    workout BOOLEAN DEFAULT false,
    income DECIMAL(15,2) DEFAULT 0,
    new_connections INTEGER DEFAULT 0,
    weight DECIMAL(5,2),
    notes TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    CONSTRAINT unique_user_date UNIQUE (user_id, date)
);

CREATE INDEX idx_daily_checkins_user_id ON daily_checkins(user_id);
CREATE INDEX idx_daily_checkins_date ON daily_checkins(user_id, date DESC);
```

### 5. Таблица `settings`

Настройки пользователей.

```sql
CREATE TABLE settings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    key VARCHAR(100) NOT NULL,
    value TEXT,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    
    CONSTRAINT unique_user_setting UNIQUE (user_id, key)
);

CREATE INDEX idx_settings_user_id ON settings(user_id);
```

### 6. Таблица `sessions` (опционально)

Для отслеживания активных сессий и статистики.

```sql
CREATE TABLE sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    started_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    last_activity TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    command_count INTEGER DEFAULT 0
);

CREATE INDEX idx_sessions_user_id ON sessions(user_id);
CREATE INDEX idx_sessions_last_activity ON sessions(last_activity DESC);
```

---

## 🔐 Система авторизации

### Вариант 1: Кастомная авторизация через Telegram ID (рекомендуется)

**Принцип работы:**
1. Пользователь отправляет команду `/start` боту
2. Бот получает `update.message.from.id` (Telegram User ID)
3. Бот проверяет наличие пользователя в БД по `telegram_user_id`
4. Если пользователя нет - создается новый пользователь
5. Все последующие запросы привязываются к этому пользователю

**Преимущества:**
- ✅ Простота реализации
- ✅ Не требует дополнительных библиотек
- ✅ Telegram уже обеспечивает аутентификацию
- ✅ Нет необходимости в паролях

**Безопасность:**
- Telegram User ID невозможно подделать (проверяется Telegram API)
- Row Level Security (RLS) в Supabase для изоляции данных
- Валидация всех запросов на уровне БД

### Вариант 2: Supabase Auth (альтернатива)

Использование встроенной системы аутентификации Supabase с кастомным провайдером.

**Недостатки:**
- Более сложная реализация
- Требует дополнительных настроек
- Избыточно для Telegram бота

---

## 🔒 Row Level Security (RLS) политики

### Политики безопасности для каждой таблицы:

```sql
-- Пользователи могут видеть только свои данные
ALTER TABLE goals ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own goals"
    ON goals FOR SELECT
    USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own goals"
    ON goals FOR INSERT
    WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own goals"
    ON goals FOR UPDATE
    USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own goals"
    ON goals FOR DELETE
    USING (auth.uid() = user_id);
```

**Примечание:** Если используем кастомную авторизацию, RLS будет работать через application-level проверки.

---

## 🔄 План миграции данных

### Этап 1: Подготовка инфраструктуры

1. **Создание проекта в Supabase**
   - Регистрация/вход в Supabase
   - Создание нового проекта
   - Получение credentials (URL, API Key, Service Role Key)

2. **Настройка окружения**
   - Добавление переменных окружения:
     ```
     SUPABASE_URL=https://xxx.supabase.co
     SUPABASE_KEY=eyJ...
     SUPABASE_SERVICE_ROLE_KEY=eyJ... (для миграций)
     ```

3. **Создание схемы БД**
   - Выполнение SQL миграций для создания таблиц
   - Настройка индексов
   - Настройка RLS (если используется)

### Этап 2: Миграция существующих данных

1. **Экспорт данных из SQLite**
   ```python
   # Скрипт для экспорта текущих данных
   # Читает goals.db и экспортирует в JSON/SQL
   ```

2. **Маппинг данных**
   - Текущий `TELEGRAM_USER_ID` → новый `user_id` в таблице `users`
   - Существующие цели → новые записи с `user_id`
   - История прогресса → новые записи с `user_id`

3. **Импорт в Supabase**
   ```python
   # Скрипт для импорта данных
   # Создает пользователя, переносит цели и историю
   ```

4. **Валидация миграции**
   - Проверка количества записей
   - Проверка целостности данных
   - Тестирование функциональности

### Этап 3: Обновление кода

1. **Создание модуля для работы с Supabase**
   ```python
   # database_supabase.py
   # Класс Database с методами для работы с Supabase
   ```

2. **Рефакторинг существующего кода**
   - Замена SQLite запросов на Supabase запросы
   - Добавление проверки пользователя во все методы
   - Обновление всех функций для работы с `user_id`

3. **Обновление команд бота**
   - Автоматическое создание пользователя при первом `/start`
   - Привязка всех команд к текущему пользователю
   - Обновление всех обработчиков команд

### Этап 4: Тестирование

1. **Unit тесты**
   - Тестирование работы с БД
   - Тестирование авторизации
   - Тестирование изоляции данных

2. **Интеграционные тесты**
   - Тестирование полного flow команд
   - Тестирование с несколькими пользователями
   - Тестирование миграции данных

3. **Production тестирование**
   - Тестирование на staging окружении
   - Проверка производительности
   - Проверка безопасности

---

## 📝 Пользовательские сценарии

### Сценарий 1: Новый пользователь

1. Пользователь находит бота в Telegram
2. Отправляет команду `/start`
3. Бот приветствует и создает аккаунт автоматически
4. Бот показывает список доступных команд
5. Пользователь может начать использовать бота

**Технические детали:**
- Проверка наличия пользователя по `telegram_user_id`
- Если нет - создание новой записи в `users`
- Инициализация дефолтных целей для нового пользователя

### Сценарий 2: Существующий пользователь

1. Пользователь отправляет команду `/start`
2. Бот проверяет наличие пользователя
3. Бот приветствует по имени
4. Пользователь продолжает работу со своими данными

### Сценарий 3: Использование команд

1. Пользователь отправляет команду (например, `/done тренировка`)
2. Бот определяет пользователя по `telegram_user_id`
3. Бот выполняет операцию с данными этого пользователя
4. Бот возвращает результат

**Важно:** Все операции изолированы по пользователю.

### Сценарий 4: Миграция существующего пользователя

1. Администратор запускает скрипт миграции
2. Скрипт читает `TELEGRAM_USER_ID` из `.env`
3. Скрипт создает пользователя в Supabase
4. Скрипт переносит все данные с привязкой к новому `user_id`
5. Пользователь продолжает работу без изменений

---

## 🔧 Технические детали реализации

### 1. Модуль работы с Supabase

```python
# Структура модуля database_supabase.py

class SupabaseDatabase:
    def __init__(self):
        # Инициализация клиента Supabase
        pass
    
    def get_or_create_user(self, telegram_user_id, telegram_data):
        # Получить или создать пользователя
        pass
    
    def get_user_goals(self, user_id):
        # Получить цели пользователя
        pass
    
    def update_goal(self, user_id, goal_id, value):
        # Обновить цель с проверкой принадлежности
        pass
    
    # ... остальные методы
```

### 2. Обновление обработчиков команд

```python
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Получаем Telegram User ID
    telegram_user_id = update.message.from.id
    
    # Получаем или создаем пользователя
    user = db.get_or_create_user(telegram_user_id, update.message.from)
    
    # Приветствуем пользователя
    await update.message.reply_text(f"Привет, {user['first_name']}!")
```

### 3. Middleware для проверки пользователя

```python
async def check_user_middleware(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Автоматическая проверка и создание пользователя
    # Сохранение user_id в context для использования в командах
    pass
```

---

## 📊 Структура проекта после миграции

```
techrisebot/
├── bot.py                 # Главный файл бота (обновлен)
├── config.py             # Конфигурация (добавлены Supabase credentials)
├── database.py           # Старый модуль (deprecated)
├── database_supabase.py  # Новый модуль для Supabase
├── goals.py              # Логика расчета (обновлена)
├── scheduler.py          # Планировщик (обновлен)
├── migrations/           # Скрипты миграции
│   ├── 001_create_tables.sql
│   ├── 002_migrate_data.py
│   └── 003_setup_rls.sql
├── tests/                # Тесты
│   ├── test_database.py
│   ├── test_auth.py
│   └── test_migration.py
├── .env                  # Переменные окружения
└── requirements.txt      # Зависимости (добавлен supabase-py)
```

---

## 🚨 Риски и митигация

### Риск 1: Потеря данных при миграции

**Митигация:**
- Создание полного бэкапа SQLite перед миграцией
- Валидация данных после миграции
- Возможность отката миграции
- Тестирование на копии данных

### Риск 2: Проблемы с производительностью

**Митигация:**
- Использование индексов в БД
- Кэширование часто используемых данных
- Мониторинг производительности запросов
- Оптимизация запросов

### Риск 3: Проблемы с безопасностью

**Митигация:**
- Валидация всех входных данных
- Проверка принадлежности данных пользователю на уровне БД
- Использование RLS политик
- Регулярные аудиты безопасности

### Риск 4: Проблемы с доступностью Supabase

**Митигация:**
- Использование connection pooling
- Обработка ошибок подключения
- Retry логика для запросов
- Мониторинг доступности сервиса

---

## 📅 План реализации

### Фаза 1: Подготовка (1-2 дня)
- [ ] Создание проекта в Supabase
- [ ] Настройка окружения
- [ ] Создание схемы БД
- [ ] Написание скриптов миграции

### Фаза 2: Разработка (3-5 дней)
- [ ] Создание модуля `database_supabase.py`
- [ ] Рефакторинг существующего кода
- [ ] Обновление обработчиков команд
- [ ] Добавление middleware для авторизации

### Фаза 3: Миграция данных (1 день)
- [ ] Экспорт данных из SQLite
- [ ] Импорт в Supabase
- [ ] Валидация миграции

### Фаза 4: Тестирование (2-3 дня)
- [ ] Unit тесты
- [ ] Интеграционные тесты
- [ ] Тестирование с несколькими пользователями
- [ ] Performance тестирование

### Фаза 5: Деплой (1 день)
- [ ] Обновление production окружения
- [ ] Миграция production данных
- [ ] Мониторинг работы системы

**Общее время: 8-12 дней**

---

## ✅ Критерии приемки

### Функциональные требования:
- [ ] Поддержка множественных пользователей
- [ ] Автоматическое создание пользователей при первом `/start`
- [ ] Изоляция данных между пользователями
- [ ] Все существующие команды работают корректно
- [ ] Миграция данных без потерь

### Нефункциональные требования:
- [ ] Время ответа бота < 2 секунд
- [ ] Доступность системы > 99%
- [ ] Безопасность данных пользователей
- [ ] Масштабируемость до 1000+ пользователей

### Технические требования:
- [ ] Код покрыт тестами (>80%)
- [ ] Документация обновлена
- [ ] Логирование всех операций
- [ ] Мониторинг работы системы

---

## 📚 Дополнительные соображения

### Масштабирование:
- Возможность горизонтального масштабирования бота
- Использование connection pooling для БД
- Кэширование часто используемых данных

### Мониторинг:
- Логирование всех операций
- Метрики производительности
- Алерты при ошибках

### Будущие улучшения:
- Веб-интерфейс для управления целями
- Экспорт данных пользователей
- Аналитика и статистика
- Интеграции с другими сервисами

---

## 🔗 Зависимости

### Новые зависимости:
```txt
supabase-py>=2.0.0
psycopg2-binary>=2.9.0  # Опционально, если нужен прямой доступ к PostgreSQL
```

### Обновление существующих:
- Все существующие зависимости остаются без изменений

---

## 📖 Документация

### Для разработчиков:
- API документация модуля `database_supabase.py`
- Описание схемы БД
- Инструкции по миграции

### Для пользователей:
- Обновление README с описанием новой функциональности
- Инструкции по использованию бота

---

## 🎯 Итоговые требования

1. **Миграция на Supabase**: Полный перенос данных и функциональности
2. **Мультипользовательская система**: Поддержка неограниченного количества пользователей
3. **Авторизация через Telegram**: Использование Telegram User ID для аутентификации
4. **Изоляция данных**: Каждый пользователь видит только свои данные
5. **Обратная совместимость**: Существующий пользователь продолжает работу без изменений

---

**Версия документа:** 1.0  
**Дата создания:** 2026-01-07  
**Статус:** Готов к реализации


