# Техническое Задание: Telegram Bot "Goals Progress Tracker 2026"

## 🎯 Цель проекта
Создать Telegram бота, который ежедневно отслеживает прогресс по целям на 2026 год и присылает уведомления о том, насколько я опережаю/отстаю от плана.

---

## 📋 Мои цели на 2026 (из Notion)

### Финансовые цели
| Цель | Дедлайн | Метрика |
|------|---------|---------|
| 1 млн ₽/мес от бизнеса | Февраль-Март | Ежемесячный доход |
| 2 млн ₽/мес от бизнеса | Май-Июнь | Ежемесячный доход |
| 5 млн ₽/мес от бизнеса | Ноябрь-Декабрь | Ежемесячный доход |
| 3-4 источника дохода | Середина года | Количество источников |

### Здоровье и спорт
| Цель | Дедлайн | Метрика |
|------|---------|---------|
| Вес 80 кг | Середина года | Текущий вес (кг) |
| 3-4 тренировки/неделю | С января | Тренировок за неделю |
| 2 марафона | Май + Сентябрь | Количество пробежанных |
| Теннис на соревнованиях | До конца года | Да/Нет |

### Крупные покупки
| Цель | Дедлайн | Сумма |
|------|---------|-------|
| Voyah Free (авто) | Середина года | Факт покупки |
| Квартира в Москве | Середина года | 25 млн ₽ |

### Путешествия
| Цель | Дедлайн | Бюджет |
|------|---------|--------|
| Лыжи Сочи | Январь | 300K ₽ |
| Турция с дочкой | Март | 300K ₽ |
| Корея/Япония с дочкой | Февраль/Апрель | 1 млн ₽ |
| 1 новая страна/месяц | Весь год | ~3 млн ₽ |

### Личные цели
| Цель | Дедлайн | Метрика |
|------|---------|---------|
| Знакомства | С января | 1-2 в неделю |

---

## 🤖 Архитектура бота

### Стек технологий
```
- Python 3.11+
- python-telegram-bot (библиотека)
- SQLite (база данных для простоты)
- APScheduler (планировщик для уведомлений)
```

### Структура проекта
```
goals-tracker-bot/
├── bot.py              # Главный файл бота
├── database.py         # Работа с SQLite
├── goals.py            # Логика расчета прогресса
├── scheduler.py        # Планировщик уведомлений
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
└── goals_data.json     # Данные о целях
```

---

## 📱 Функционал бота

### Команды

#### /start
Приветствие и краткая инструкция

#### /today
Показать сводку на сегодня:
- Какой сейчас день года (например: "День 7 из 365")
- Общий прогресс по всем целям
- Что должно быть достигнуто к этой дате
- Фактический статус

#### /goals
Список всех целей с их статусом:
```
📊 МОИ ЦЕЛИ 2026

💰 ФИНАНСЫ
├─ 1М/мес → Дедлайн: 28 фев → ⏳ Осталось 52 дня
├─ 2М/мес → Дедлайн: 31 мая → ⏳ Осталось 144 дня
└─ Доп. источники: 1/4 ✓

🏃 СПОРТ
├─ Вес: 87кг → Цель: 80кг → -7кг осталось
├─ Тренировки: 2/4 на этой неделе
└─ Марафоны: 0/2

...
```

#### /update [категория] [значение]
Обновить прогресс по цели:
- `/update вес 85` — обновить текущий вес
- `/update доход 500000` — обновить доход за месяц
- `/update тренировки 3` — добавить тренировки за неделю
- `/update страны +1` — добавить посещенную страну

#### /log
Добавить запись в дневник прогресса (свободный текст)

#### /report
Недельный отчет с графиками (текстовыми):
```
📈 ОТЧЕТ ЗА НЕДЕЛЮ

Прогресс по финансам:
▓▓▓▓▓░░░░░ 50% к цели февраля

Прогресс по весу:
▓▓▓▓▓▓▓░░░ 70% (было 87, стало 85)

Тренировки:
Пн ✅ Вт ✅ Ср ❌ Чт ✅ Пт ❌ Сб ❌ Вс ✅
3/4 = 75%
```

#### /remind [on/off]
Включить/выключить ежедневные напоминания

---

## ⏰ Система уведомлений

### Утреннее уведомление (9:00)
```
☀️ Доброе утро! День 7/365

🎯 Фокус на сегодня:
• До дедлайна "1М/мес" осталось 52 дня
• Тренировок на этой неделе: 2/4

💪 Ты в графике по: спорт, путешествия
⚠️ Нужно ускориться: финансы

Хорошего дня!
```

### Вечернее уведомление (21:00)
```
🌙 Как прошел день?

Ответь на вопросы:
1. Была ли тренировка? (да/нет)
2. Сколько заработал сегодня? (число)
3. Новые знакомства? (да/нет)

Или напиши /skip чтобы пропустить
```

### Еженедельное уведомление (Воскресенье, 20:00)
Полный отчет за неделю с рекомендациями

---

## 🗄️ База данных SQLite

### Таблица goals
```sql
CREATE TABLE goals (
    id INTEGER PRIMARY KEY,
    category TEXT,           -- финансы/спорт/покупки/путешествия/личное
    name TEXT,               -- название цели
    target_value REAL,       -- целевое значение
    current_value REAL,      -- текущее значение
    unit TEXT,               -- единица измерения
    deadline DATE,           -- дедлайн
    created_at DATETIME,
    updated_at DATETIME
);
```

### Таблица progress_log
```sql
CREATE TABLE progress_log (
    id INTEGER PRIMARY KEY,
    goal_id INTEGER,
    value REAL,
    note TEXT,
    logged_at DATETIME,
    FOREIGN KEY (goal_id) REFERENCES goals(id)
);
```

### Таблица daily_checkins
```sql
CREATE TABLE daily_checkins (
    id INTEGER PRIMARY KEY,
    date DATE UNIQUE,
    workout BOOLEAN,
    income REAL,
    new_connections INTEGER,
    weight REAL,
    notes TEXT,
    created_at DATETIME
);
```

---

## 🚀 Инструкция по запуску

### Шаг 1: Создать бота в Telegram
1. Открой @BotFather в Telegram
2. Отправь `/newbot`
3. Придумай имя (например: "Goals Tracker 2026")
4. Придумай username (например: `ivan_goals_2026_bot`)
5. Скопируй токен (выглядит как `123456:ABC-DEF...`)

### Шаг 2: Настроить проект
1. Создай файл `.env`:
```
TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
TELEGRAM_USER_ID=твой_telegram_id
```

2. Чтобы узнать свой Telegram ID:
   - Напиши боту @userinfobot команду /start
   - Он покажет твой ID

### Шаг 3: Установить зависимости
```bash
pip install -r requirements.txt
```

### Шаг 4: Запустить бота
```bash
python bot.py
```

### Шаг 5: Для постоянной работы (на сервере)
Использовать systemd или запустить через:
```bash
nohup python bot.py &
```

---

## 📝 Файл requirements.txt

```
python-telegram-bot==20.7
APScheduler==3.10.4
python-dotenv==1.0.0
```

---

## 🎨 Пример кода bot.py (базовая структура)

```python
import os
import logging
from datetime import datetime, date
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from dotenv import load_dotenv

load_dotenv()

# Конфигурация
TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
USER_ID = int(os.getenv('TELEGRAM_USER_ID'))

# Логирование
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Данные о целях (позже перенести в БД)
GOALS = {
    'финансы': {
        'income_1m': {'target': 1000000, 'current': 0, 'deadline': '2026-02-28', 'name': '1М/мес'},
        'income_2m': {'target': 2000000, 'current': 0, 'deadline': '2026-05-31', 'name': '2М/мес'},
        'income_5m': {'target': 5000000, 'current': 0, 'deadline': '2026-11-30', 'name': '5М/мес'},
    },
    'спорт': {
        'weight': {'target': 80, 'current': 87, 'deadline': '2026-06-30', 'name': 'Вес'},
        'workouts_week': {'target': 4, 'current': 0, 'deadline': None, 'name': 'Тренировки/нед'},
        'marathons': {'target': 2, 'current': 0, 'deadline': '2026-09-30', 'name': 'Марафоны'},
    },
    'покупки': {
        'car': {'target': 1, 'current': 0, 'deadline': '2026-06-30', 'name': 'Voyah Free'},
        'apartment': {'target': 25000000, 'current': 0, 'deadline': '2026-06-30', 'name': 'Квартира'},
    },
    'путешествия': {
        'countries': {'target': 12, 'current': 0, 'deadline': '2026-12-31', 'name': 'Новые страны'},
    },
}

def days_until(deadline_str):
    """Дней до дедлайна"""
    if not deadline_str:
        return None
    deadline = datetime.strptime(deadline_str, '%Y-%m-%d').date()
    return (deadline - date.today()).days

def day_of_year():
    """Какой сейчас день года"""
    return date.today().timetuple().tm_yday

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "🎯 Привет! Я твой трекер целей на 2026.\n\n"
        "Команды:\n"
        "/today - сводка на сегодня\n"
        "/goals - все цели\n"
        "/update - обновить прогресс\n"
        "/report - недельный отчет"
    )

async def today(update: Update, context: ContextTypes.DEFAULT_TYPE):
    day = day_of_year()
    msg = f"📅 День {day}/365 ({round(day/365*100)}% года прошло)\n\n"
    
    msg += "⏰ Ближайшие дедлайны:\n"
    for cat, goals in GOALS.items():
        for key, goal in goals.items():
            days = days_until(goal.get('deadline'))
            if days and 0 < days < 60:
                msg += f"• {goal['name']}: {days} дней\n"
    
    await update.message.reply_text(msg)

async def goals(update: Update, context: ContextTypes.DEFAULT_TYPE):
    msg = "📊 МОИ ЦЕЛИ 2026\n\n"
    
    for cat, cat_goals in GOALS.items():
        emoji = {'финансы': '💰', 'спорт': '🏃', 'покупки': '🛒', 'путешествия': '✈️'}.get(cat, '📌')
        msg += f"{emoji} {cat.upper()}\n"
        
        for key, goal in cat_goals.items():
            progress = goal['current'] / goal['target'] * 100 if goal['target'] else 0
            bar = '▓' * int(progress/10) + '░' * (10 - int(progress/10))
            msg += f"├─ {goal['name']}: {bar} {progress:.0f}%\n"
        msg += "\n"
    
    await update.message.reply_text(msg)

async def send_morning_reminder(context: ContextTypes.DEFAULT_TYPE):
    """Утреннее напоминание"""
    day = day_of_year()
    msg = f"☀️ Доброе утро! День {day}/365\n\n🎯 Фокус на сегодня:\n"
    
    # Добавить актуальные цели
    for cat, goals in GOALS.items():
        for key, goal in goals.items():
            days = days_until(goal.get('deadline'))
            if days and 0 < days < 30:
                msg += f"• {goal['name']}: осталось {days} дней\n"
    
    msg += "\n💪 Хорошего дня!"
    await context.bot.send_message(chat_id=USER_ID, text=msg)

async def send_evening_reminder(context: ContextTypes.DEFAULT_TYPE):
    """Вечернее напоминание"""
    msg = (
        "🌙 Как прошел день?\n\n"
        "Обнови прогресс:\n"
        "/update вес [число]\n"
        "/update тренировка да\n"
        "/update доход [число]"
    )
    await context.bot.send_message(chat_id=USER_ID, text=msg)

def main():
    # Создаем приложение
    app = Application.builder().token(TOKEN).build()
    
    # Добавляем обработчики команд
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("today", today))
    app.add_handler(CommandHandler("goals", goals))
    
    # Настраиваем планировщик
    scheduler = AsyncIOScheduler()
    scheduler.add_job(send_morning_reminder, 'cron', hour=9, minute=0, args=[app])
    scheduler.add_job(send_evening_reminder, 'cron', hour=21, minute=0, args=[app])
    scheduler.start()
    
    # Запускаем бота
    logger.info("Бот запущен!")
    app.run_polling()

if __name__ == '__main__':
    main()
```

---

## ✅ Чеклист для Cursor

1. [ ] Создать структуру проекта
2. [ ] Реализовать подключение к Telegram API
3. [ ] Создать SQLite базу данных
4. [ ] Реализовать все команды бота
5. [ ] Настроить планировщик уведомлений
6. [ ] Добавить логику расчета прогресса
7. [ ] Протестировать локально
8. [ ] Написать инструкцию по деплою

---

## 🔧 Дополнительные улучшения (по желанию)

1. **Интеграция с Notion API** — автоматически синхронизировать цели
2. **Графики** — генерировать изображения с графиками прогресса
3. **Экспорт** — выгрузка данных в CSV/PDF
4. **Веб-дашборд** — простая веб-страница с визуализацией
5. **AI-анализ** — использовать Claude API для умных рекомендаций

---

## 💬 Промпт для Cursor

Скопируй это в Cursor:

```
Создай Telegram бота для отслеживания моих целей на 2026 год.

Требования:
1. Python + python-telegram-bot + SQLite + APScheduler
2. Команды: /start, /today, /goals, /update, /report
3. Ежедневные уведомления в 9:00 и 21:00 (МСК)
4. База данных для хранения прогресса

Цели для трекинга:
- Доход: 1М к февралю, 2М к маю, 5М к ноябрю
- Вес: с 87 до 80 кг к середине года
- Тренировки: 4 в неделю
- Марафоны: 2 штуки (май, сентябрь)
- Путешествия: 12 новых стран за год
- Покупки: машина и квартира к середине года

Сделай код понятным для новичка, с комментариями.
Добавь файл README.md с инструкцией по запуску.
```
