# 🔄 Flow диаграммы и сценарии использования

## 1. Flow авторизации пользователя

```
┌─────────────┐
│   User      │
│  Telegram   │
└──────┬──────┘
       │
       │ /start
       ▼
┌─────────────────┐
│  Bot receives   │
│  update message │
└──────┬──────────┘
       │
       │ Extract telegram_user_id
       │ from update.message.from.id
       ▼
┌─────────────────┐
│ Check if user   │
│ exists in DB    │
└──────┬──────────┘
       │
       ├─── YES ────┐
       │            │
       │            ▼
       │    ┌──────────────┐
       │    │ Get user     │
       │    │ from DB      │
       │    └──────┬───────┘
       │           │
       │           ▼
       │    ┌──────────────┐
       │    │ Welcome      │
       │    │ user by name │
       │    └──────────────┘
       │
       └─── NO ─────┐
                    │
                    ▼
            ┌──────────────┐
            │ Create new   │
            │ user in DB    │
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │ Initialize   │
            │ default goals│
            └──────┬───────┘
                   │
                   ▼
            ┌──────────────┐
            │ Welcome new  │
            │ user         │
            └──────────────┘
```

## 2. Flow выполнения команды

```
┌─────────────┐
│   User      │
│  sends cmd  │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Bot receives    │
│ command         │
└──────┬──────────┘
       │
       │ Extract telegram_user_id
       ▼
┌─────────────────┐
│ Get or create   │
│ user            │
└──────┬──────────┘
       │
       │ user_id stored in context
       ▼
┌─────────────────┐
│ Execute command │
│ with user_id    │
└──────┬──────────┘
       │
       ├─── /done ────┐
       │              │
       ├─── /update ──┤
       │              │
       ├─── /goals ───┤
       │              │
       └─── /today ───┤
                      │
                      ▼
              ┌──────────────┐
              │ Query DB     │
              │ with user_id │
              │ filter       │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ Return       │
              │ user-specific│
              │ response     │
              └──────────────┘
```

## 3. Flow миграции данных

```
┌─────────────────┐
│  SQLite DB      │
│  (Current)      │
└──────┬──────────┘
       │
       │ Export script
       ▼
┌─────────────────┐
│  Export data    │
│  to JSON/SQL    │
└──────┬──────────┘
       │
       │ Read TELEGRAM_USER_ID
       │ from .env
       ▼
┌─────────────────┐
│  Create user    │
│  in Supabase    │
└──────┬──────────┘
       │
       │ user_id generated
       ▼
┌─────────────────┐
│  Map old data   │
│  to new schema  │
└──────┬──────────┘
       │
       │ For each table:
       │ - goals
       │ - progress_log
       │ - daily_checkins
       │ - settings
       ▼
┌─────────────────┐
│  Insert data    │
│  with user_id   │
│  into Supabase  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Validate       │
│  migration      │
└──────┬──────────┘
       │
       ├─── Success ──┐
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Migration    │
       │        │ complete     │
       │        └──────────────┘
       │
       └─── Error ─────┐
                        │
                        ▼
                 ┌──────────────┐
                 │ Rollback or  │
                 │ fix issues   │
                 └──────────────┘
```

## 4. Flow обновления цели

```
┌─────────────┐
│   User      │
│ /done вес 85│
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Extract user_id │
│ from context    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Find goal       │
│ WHERE user_id = │
│ AND name = 'Вес'│
└──────┬──────────┘
       │
       ├─── Found ────┐
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Update goal   │
       │        │ current_value │
       │        └──────┬────────┘
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Insert into   │
       │        │ progress_log  │
       │        └──────┬────────┘
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Calculate    │
       │        │ progress %   │
       │        └──────┬───────┘
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Send response│
       │        │ to user      │
       │        └──────────────┘
       │
       └─── Not Found ──┐
                        │
                        ▼
                 ┌──────────────┐
                 │ Create goal  │
                 │ with default │
                 │ values       │
                 └──────────────┘
```

## 5. Flow получения списка целей

```
┌─────────────┐
│   User      │
│  /goals     │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Extract user_id │
│ from context    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Query goals     │
│ WHERE user_id = │
│ ORDER BY        │
│ category,       │
│ deadline        │
└──────┬──────────┘
       │
       │ Results filtered
       │ by user_id
       ▼
┌─────────────────┐
│ Calculate       │
│ progress for    │
│ each goal       │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Format response │
│ with emojis     │
│ and progress    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Send formatted  │
│ list to user    │
└─────────────────┘
```

## 6. Flow планировщика уведомлений

```
┌─────────────────┐
│  Scheduler      │
│  (9:00 МСК)     │
└──────┬──────────┘
       │
       │ Cron trigger
       ▼
┌─────────────────┐
│ Query all       │
│ active users    │
│ WHERE           │
│ is_active = true│
└──────┬──────────┘
       │
       │ For each user:
       ▼
┌─────────────────┐
│ Check user      │
│ notification    │
│ settings        │
└──────┬──────────┘
       │
       ├─── Enabled ──┐
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Get user     │
       │        │ goals        │
       │        └──────┬───────┘
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Calculate    │
       │        │ daily summary│
       │        └──────┬───────┘
       │               │
       │               ▼
       │        ┌──────────────┐
       │        │ Send message │
       │        │ to user via  │
       │        │ telegram_id  │
       │        └──────────────┘
       │
       └─── Disabled ──┐
                       │
                       ▼
                ┌──────────────┐
                │ Skip user    │
                └──────────────┘
```

## 7. Flow создания нового пользователя

```
┌─────────────────┐
│  First /start   │
│  command        │
└──────┬──────────┘
       │
       │ Extract telegram_user_id
       ▼
┌─────────────────┐
│ Check if user   │
│ exists          │
└──────┬──────────┘
       │
       └─── NOT EXISTS ──┐
                          │
                          ▼
                   ┌──────────────┐
                   │ Extract user │
                   │ data from    │
                   │ Telegram     │
                   │ update       │
                   └──────┬───────┘
                          │
                          │ Data:
                          │ - telegram_user_id
                          │ - first_name
                          │ - last_name
                          │ - username
                          │ - language_code
                          ▼
                   ┌──────────────┐
                   │ Insert into  │
                   │ users table  │
                   └──────┬───────┘
                          │
                          │ user_id generated
                          ▼
                   ┌──────────────┐
                   │ Initialize   │
                   │ default goals│
                   │ for user     │
                   └──────┬───────┘
                          │
                          │ Insert default goals:
                          │ - Финансы
                          │ - Спорт
                          │ - Покупки
                          │ - Путешествия
                          ▼
                   ┌──────────────┐
                   │ Set default  │
                   │ settings:    │
                   │ notifications│
                   │ = 'on'      │
                   └──────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │ Send welcome │
                   │ message      │
                   └──────────────┘
```

## 8. Flow безопасности и валидации

```
┌─────────────────┐
│  Any DB query   │
└──────┬──────────┘
       │
       │ Must include user_id
       ▼
┌─────────────────┐
│ Validate        │
│ user_id exists  │
└──────┬──────────┘
       │
       ├─── Valid ────┐
       │              │
       │              ▼
       │       ┌──────────────┐
       │       │ Check RLS    │
       │       │ policies     │
       │       └──────┬───────┘
       │              │
       │              ├─── Allowed ──┐
       │              │               │
       │              │               ▼
       │              │       ┌──────────────┐
       │              │       │ Execute query │
       │              │       └──────────────┘
       │              │
       │              └─── Denied ───┐
       │                              │
       │                              ▼
       │                       ┌──────────────┐
       │                       │ Return error  │
       │                       └──────────────┘
       │
       └─── Invalid ───┐
                       │
                       ▼
                ┌──────────────┐
                │ Return      │
                │ auth error  │
                └──────────────┘
```

## 9. Flow обработки ошибок

```
┌─────────────────┐
│  Command        │
│  execution      │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Try to execute  │
│ command         │
└──────┬──────────┘
       │
       ├─── Success ────┐
       │                 │
       │                 ▼
       │          ┌──────────────┐
       │          │ Return       │
       │          │ result       │
       │          └──────────────┘
       │
       └─── Error ──────┐
                         │
                         ▼
                  ┌──────────────┐
                  │ Catch        │
                  │ exception    │
                  └──────┬───────┘
                         │
                         │ Error types:
                         ├─── DB connection
                         ├─── Invalid user
                         ├─── Data validation
                         └─── Unknown
                         │
                         ▼
                  ┌──────────────┐
                  │ Log error    │
                  │ with context │
                  └──────┬───────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Send user    │
                  │ friendly     │
                  │ error message│
                  └──────────────┘
```

## 10. Flow синхронизации данных

```
┌─────────────────┐
│  User action    │
│  (update goal)  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Update in       │
│ Supabase        │
└──────┬──────────┘
       │
       │ Transaction:
       │ 1. Update goal
       │ 2. Insert log
       │ 3. Update timestamp
       ▼
┌─────────────────┐
│ Commit          │
│ transaction     │
└──────┬──────────┘
       │
       ├─── Success ────┐
       │                 │
       │                 ▼
       │          ┌──────────────┐
       │          │ Data synced  │
       │          │ immediately  │
       │          └──────────────┘
       │
       └─── Error ──────┐
                         │
                         ▼
                  ┌──────────────┐
                  │ Rollback     │
                  │ transaction  │
                  └──────┬───────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Retry or     │
                  │ notify user │
                  └──────────────┘
```

---

## 📋 Детальные сценарии использования

### Сценарий 1: Первый запуск бота новым пользователем

**Шаги:**
1. Пользователь находит бота в Telegram
2. Пользователь отправляет `/start`
3. Бот получает `update.message.from.id = 123456789`
4. Бот проверяет наличие пользователя в БД: `SELECT * FROM users WHERE telegram_user_id = 123456789`
5. Пользователь не найден
6. Бот создает нового пользователя:
   ```sql
   INSERT INTO users (telegram_user_id, first_name, last_name, telegram_username)
   VALUES (123456789, 'Иван', 'Иванов', 'ivan_user')
   ```
7. Бот получает `user_id = 'uuid-123'`
8. Бот инициализирует дефолтные цели для пользователя
9. Бот отправляет приветственное сообщение

**Ожидаемый результат:**
- Пользователь создан в БД
- Дефолтные цели созданы
- Пользователь получил приветствие

### Сценарий 2: Существующий пользователь использует команду

**Шаги:**
1. Пользователь отправляет `/done тренировка`
2. Бот получает `telegram_user_id = 123456789`
3. Бот находит пользователя: `SELECT id FROM users WHERE telegram_user_id = 123456789`
4. Бот получает `user_id = 'uuid-123'`
5. Бот находит цель тренировок: `SELECT * FROM goals WHERE user_id = 'uuid-123' AND name LIKE '%Тренировки%'`
6. Бот обновляет значение: `UPDATE goals SET current_value = current_value + 1 WHERE id = 'goal-uuid'`
7. Бот создает запись в логе: `INSERT INTO progress_log (goal_id, user_id, value) VALUES (...)`
8. Бот отправляет подтверждение пользователю

**Ожидаемый результат:**
- Цель обновлена только для этого пользователя
- Запись в логе создана
- Пользователь получил подтверждение

### Сценарий 3: Два пользователя используют бота одновременно

**Пользователь A (telegram_user_id = 111):**
1. Отправляет `/done тренировка`
2. Бот обновляет тренировки для user_id_A

**Пользователь B (telegram_user_id = 222):**
1. Отправляет `/done тренировка` одновременно
2. Бот обновляет тренировки для user_id_B

**Ожидаемый результат:**
- Данные пользователей изолированы
- Каждый пользователь видит только свои данные
- Нет конфликтов между пользователями

### Сценарий 4: Миграция существующих данных

**Шаги:**
1. Администратор запускает скрипт миграции
2. Скрипт читает `TELEGRAM_USER_ID = 123456789` из `.env`
3. Скрипт создает пользователя в Supabase
4. Скрипт экспортирует данные из SQLite:
   - Все цели
   - Вся история прогресса
   - Все ежедневные отметки
   - Все настройки
5. Скрипт импортирует данные в Supabase с привязкой к новому `user_id`
6. Скрипт валидирует миграцию:
   - Количество записей совпадает
   - Данные корректны
   - Связи сохранены
7. Миграция завершена

**Ожидаемый результат:**
- Все данные перенесены без потерь
- Пользователь может продолжить работу
- История сохранена

---

## 🔐 Сценарии безопасности

### Сценарий 1: Попытка доступа к чужим данным

**Попытка:**
Пользователь A пытается получить данные пользователя B через прямой SQL запрос.

**Защита:**
- Все запросы фильтруются по `user_id`
- RLS политики на уровне БД
- Валидация на уровне приложения

**Результат:**
Пользователь A получает только свои данные.

### Сценарий 2: Подделка Telegram User ID

**Попытка:**
Злоумышленник пытается подделать `telegram_user_id` в запросе.

**Защита:**
- Telegram User ID проверяется через Telegram API
- Невозможно подделать без доступа к аккаунту Telegram
- Все запросы идут через официальный Telegram Bot API

**Результат:**
Подделка невозможна.

---

## 📊 Метрики и мониторинг

### Метрики для отслеживания:

1. **Количество пользователей**
   - Новые регистрации в день
   - Активные пользователи
   - Общее количество пользователей

2. **Использование команд**
   - Количество команд в день
   - Популярные команды
   - Среднее время ответа

3. **Производительность БД**
   - Время выполнения запросов
   - Количество запросов в секунду
   - Использование connection pool

4. **Ошибки**
   - Количество ошибок в день
   - Типы ошибок
   - Время восстановления

---

**Версия документа:** 1.0  
**Дата создания:** 2026-01-07


